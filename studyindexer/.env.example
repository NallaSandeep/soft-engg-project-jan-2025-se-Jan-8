# Environment Configuration
# -------------------

# Core Settings
APP_ENV=development                 # Options: development, testing, production
DEBUG=true                          # Enable debug mode and API docs
VERSION=0.1.0                      # Application version
HOST=0.0.0.0                       # Host to bind the server to
PORT=8080                          # Port to run the FastAPI server on
FASTAPI_HEALTH_ENDPOINT=/health    # Health check endpoint path

# Security Settings
# ----------------
# WARNING: Change these values in production!
API_KEY=studyhub_dev_api_key_2024  # API key for service authentication
JWT_SECRET=studyhub_development_jwt_secret_key_32chars  # Min 32 characters
JWT_ALGORITHM=HS256                # JWT signing algorithm
JWT_TOKEN_PREFIX=Bearer            # Authorization header prefix
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440 # Token expiration time (24 hours)

# Rate Limiting
# ------------
RATE_LIMIT_ENABLED=true           # Enable/disable rate limiting
RATE_LIMIT_CALLS=100              # Number of calls allowed
RATE_LIMIT_PERIOD=3600            # Time period in seconds

# CORS Configuration
# ----------------
CORS_ORIGINS=http://localhost:3000,http://localhost:5000  # Allowed origins

# Vector Database (ChromaDB)
# ------------------------
CHROMA_HOST=0.0.0.0               # ChromaDB server binding host
CHROMA_PORT=8000                  # ChromaDB server port
CHROMA_PERSIST_DIR=./data/chroma  # Data persistence directory
CHROMA_REQUIRED=true              # Whether ChromaDB is required
CHROMA_STARTUP_TIME=15            # Time to wait after starting ChromaDB (seconds)
CHROMA_HEALTH_ENDPOINT=/api/v1/heartbeat  # Health check endpoint path

# Document Processing
# -----------------
MAX_CHUNK_SIZE=1000              # Maximum text chunk size
CHUNK_OVERLAP=200                # Overlap between chunks
MAX_FILE_SIZE=16777216          # Maximum file size (16MB)

# Supported MIME types (comma-separated)
SUPPORTED_FILE_TYPES=["application/pdf", "text/plain", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "text/markdown"]

# Vector Embedding
# --------------
EMBEDDING_MODEL=all-MiniLM-L6-v2  # SentenceTransformer model
EMBEDDING_DEVICE=cpu              # Device for inference (cpu/cuda)

# Storage Paths
# ------------
UPLOAD_DIR=./uploads             # Temporary upload storage
PROCESSED_DIR=./data/processed   # Processed document storage
TEMP_DIR=./data/temp            # Temporary file storage
LOG_DIR=./logs                  # Application logs

# Service Manager Settings
# ----------------------
SERVICE_START_ORDER=["chromadb", "fastapi"]  # Order to start services
HEALTH_CHECK_TIMEOUT=10        # Timeout for health checks (seconds)
HEALTH_CHECK_INTERVAL=60       # Interval for continuous health monitoring (seconds)

# Logging
# -------
LOG_LEVEL=INFO                 # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s 